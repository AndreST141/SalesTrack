MYSQL_ROOT_PASSWORD=root
DB_NAME=salestrack
MYSQL_PORT=3307
BACKEND_PORT=5000
FRONTEND_PORT=5173
VITE_API_URL=http://localhost:5000/api
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174

# Segredo TOTP (padrão Google Authenticator/Authy) do usuário TECNICO (login: TECNICO).
# Precisa ser uma string base32 válida. Gere uma nova com:
#   docker compose exec backend python -c "import pyotp; print(pyotp.random_base32())"
# Depois rode scripts/gerar_codigo_tecnico.py para cadastrar no app autenticador.
# OBRIGATÓRIO trocar pelo valor gerado e mantê-lo em sigilo — nunca commitar o valor real.
TECNICO_SECRET=troque-por-um-segredo-base32-gerado-com-pyotp
